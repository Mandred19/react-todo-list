import { Components } from '@mui/material/styles/components';
import { Theme } from '@mui/material';

export const MuiPaper: Components<Theme> = {
  MuiPaper: {
    styleOverrides: {
      root: {
        backgroundColor: '#eee',
      },
    },
  },
};
